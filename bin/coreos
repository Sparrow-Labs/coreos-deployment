#!/usr/bin/env ruby
# -*- mode: ruby -*-

require 'thor'

class MyCLI < Thor
  desc "deploy HOST", "deploys all services in ./coreos/HOST/*.service to HOST"
  def deploy(host)
    puts "Hello #{host}"
  end
end
